# Verification Config for IES (Base Router)
# Simulating a config where interfaces are top-level or low indentation

interface "system"
    address 10.0.0.1/32
    description "System Loopback"
    no shutdown
exit

interface "to-PE2"
    address 10.1.2.1/30
    port 1/1/1
    description "Link to PE2"
    no shutdown
exit

service
    epipe 100 customer 1 create
        description "Test Epipe"
        sap 1/1/2:100 create
        exit
        no shutdown
    exit
exit
