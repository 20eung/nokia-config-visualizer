# Release Notes - v5.7.3

> **Date**: 2026-04-10
> **Branch**: `main`
> **Type**: Enhancement

## 변경 사항

### UX 개선

- **Show Code → Copy Code 버튼 변환** (`src/components/v3/ServiceDiagram.tsx`)
  - 기존 2단계 동작(Show Code → Copy) → 1단계(Copy Code 클릭 즉시 복사)
  - 복사 성공 시 2초간 초록색 `Copied!` 피드백 표시 (Copy PNG 버튼과 동일한 UX)
  - Mermaid 코드 패널(다크 박스) 제거
  - `navigator.clipboard` API + `execCommand` fallback 지원
