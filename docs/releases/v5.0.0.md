# Release Notes - v5.0.0

> **Date**: 2026-03-01
> **Branch**: `main`
> **Type**: Major Release (Breaking Changes)

## Summary

v4 레거시를 완전히 제거하고 Tailwind CSS v4로 전면 마이그레이션한 메이저 릴리즈. 사이트별 대시보드, 다크모드, 보안 강화가 포함된 완전한 플랫폼 재설계입니다.

## Breaking Changes

- **V1 레거시 제거**: V1Page, nokiaParser, TopologyEngine 등 7개 파일 삭제. `/v1` 경로 제거
- **Vanilla CSS 제거**: 11개 CSS 파일(~4,800줄) 삭제, Tailwind CSS utility class로 전면 마이그레이션

## New Features

### 사이트별 대시보드

- Config 로드 후 첫 화면으로 서비스 통계 카드 + 사이트 카드 그리드 표시
- Hostname에서 사이트명 자동 추출 (`siteGrouper` 유틸리티)
- HA 페어 자동 감지 (2대 이상 장비 사이트)
- 사이트 카드 클릭 → 해당 사이트 서비스 자동 선택 + Services 뷰 전환
- 사이트명/hostname 검색 필터
- Dashboard / Services 모드 원클릭 토글

### 다크모드

- Sun/Moon 토글 버튼
- localStorage 저장 + `prefers-color-scheme` 자동 감지
- NOC/관제센터 야간 근무 최적화
- 다이어그램 영역은 bg-white 유지 (Mermaid SVG 호환)

### 보안 강화

- **CORS 제한**: `*` → 환경변수 기반 도메인 설정
- **API Key 인증**: X-API-Key 헤더 기반 미들웨어 (`server/src/middleware/auth.ts`)
- **Path Traversal 방어**: watch-folder 경로 화이트리스트 + 심볼릭 링크 차단
- **WebSocket Origin 검증**: verifyClient 콜백으로 origin 검증
- **글로벌 API Rate Limiting**: /api 전체에 90 req/min 제한
- **Health 정보 은닉**: `{"status":"ok"}` 만 반환
- **CSP 헤더**: nginx.conf에 Content-Security-Policy + Referrer-Policy 추가

### UI/UX

- Tailwind CSS v4 도입 (`@tailwindcss/vite` 플러그인 기반)
- 전체 UI에 dark: 변형 적용 (배경, 텍스트, 보더)
- Mermaid QoS CSS 분리: `src/styles/mermaid-overrides.css`

### 성능 최적화

- Config 로딩 배치화: 89개 파일 동시 fetch → 10개씩 배치 fetch
- `@tanstack/react-virtual` 설치 (서비스 리스트 가상화 준비)

## Files Changed

| Category | Count | Detail |
|----------|-------|--------|
| 삭제 | 18개 | V1 7개 + CSS 11개 |
| 신규 | 6개 | Dashboard, useDarkMode, siteGrouper, site.ts, mermaid-overrides.css, auth.ts |
| 수정 | 19개 | Tailwind 변환, 보안 강화, Docker 설정 |
| 총 변경 | +2,442 / -6,126줄 | 순 3,684줄 감소 |
