# Release Notes v1.8.0

## 🎯 VRRP 기반 HA 탐지 기능

### 새로운 기능

#### VRRP 이중화 자동 탐지
- **VRRP VIP 기반 HA Pair 인식**: 동일한 VRRP Virtual IP를 공유하는 인터페이스들을 자동으로 이중화 쌍으로 탐지
- **향상된 HA 필터**: Static Route 기반 이중화뿐만 아니라 VRRP 기반 이중화도 "이중화" 필터에서 자동 선택
- **완전한 Peer IP 추론**: /30 서브넷뿐만 아니라 VRRP가 설정된 모든 서브넷에서 peer IP 자동 추론

### 주요 개선사항

#### TopologyEngine
- VRRP VIP별 인터페이스 그룹화 로직 추가
- HA pairs 배열에 VRRP 타입 쌍 자동 등록
- 상세한 디버그 로깅 (Static Route vs VRRP 구분)

#### Peer Detection
- `findPeerAndRoutes` 함수에 VRRP 기반 peer 탐지 추가
- VRRP VIP를 제외한 서브넷 내 peer IP 자동 계산
- /29, /28 등 다양한 서브넷에서 작동

#### HA Filter
- 전체 devices 배열 순회로 검색 필터와 무관하게 모든 HA 인터페이스 탐지
- 인터페이스 자체 IP와 peer IP 양방향 검사
- 선택된 인터페이스 상세 로깅

### 기술적 세부사항

**변경된 파일**:
- `src/utils/TopologyEngine.ts`: VRRP HA pair 탐지 로직 (+47 lines)
- `src/utils/mermaidGenerator.ts`: VRRP peer 추론 (+12 lines)
- `src/components/InterfaceList.tsx`: HA 필터 개선 (+5, -16 lines)

**콘솔 로그 예시**:
```
✅ [HA Detection] VRRP HA Pair found: VIP 10.10.30.217 
   → Router_BB3:p3/1/13 (10.10.30.218, priority: 110) 
   & Router_BB4:p3/2/12 (10.10.30.219, priority: default)
🎯 [HA Detection] Total HA Pairs detected: 2 (Static Route: 1, VRRP: 1)
```

### 사용 예시

1. VRRP로 이중화된 인터페이스가 있는 config 파일 업로드
2. "Start Analysis" 클릭
3. 브라우저 콘솔에서 VRRP HA Pair 탐지 확인
4. "이중화" 필터 클릭 → VRRP 쌍 자동 선택

### 호환성

- 기존 Static Route 기반 HA 탐지 기능 완전 유지
- 추가 설정 없이 VRRP 이중화 자동 인식
- 모든 기존 기능과 100% 호환

---

## 🔒 보안 개선 (이전 커밋)

- Config 파일의 모든 민감 정보 익명화 완료
- 실제 회사명 → Customer1~10으로 변경
- 공인 IP → 사설 IP (10.x.x.x) 변경
- 공개 저장소 데모용으로 안전하게 정리

---

**릴리즈 날짜**: 2025-12-17
**버전**: v1.8.0
**이전 버전**: v1.7.0
