---
feature: dictionary-structure-v5
version: v5.0.0
status: planning
created: 2026-02-16
author: Claude Code
---

# Dictionary Structure v5.0 - Plan Document

> **Feature**: 이름 사전 구조 v5.0 - 양방향 검색 지원
> **Version**: v5.0.0
> **Status**: 📋 Planning
> **Type**: Data Model Enhancement (Bidirectional Search)

---

## 1. Feature Overview

### 1.1 Summary

현재 이름 사전(v4.3)은 `originalToken` + `aliases` 2개 필드로 구성되어 있지만, **Config 검색 키워드**와 **사용자 검색어**가 혼재되어 검색 로직이 불명확합니다. v5.0에서는 **3개 필드 구조**로 역할을 명확히 분리하여 양방향 검색을 지원합니다.

### 1.2 Key Objectives

1. **역할 명확화**: Config 검색용 키워드와 사용자 검색어 분리
2. **양방향 검색**: configKeywords도 사용자 입력으로 검색 가능
3. **다중 Config 키워드 지원**: originalToken 1개 제약 제거
4. **검색 정확도 향상**: "SK쉴더스" 검색 → 모든 관련 Config 키워드(Bizen, ADTCAPS, SKShielders, Infosec) 검색
5. **AI 프롬프트 개선**: 검색 의도를 명확히 전달

### 1.3 Current vs Proposed Structure

**현재 구조 (v4.3)**:
```typescript
interface DictionaryEntry {
  id: string;
  originalToken: string;        // 단일 토큰만 가능
  aliases: string[];            // Config 키워드 + 사용자 검색어 혼재
  autoGenerated: boolean;
  userEdited: boolean;
}
```

**문제점**:
- originalToken은 1개만 → 연관된 여러 Config 키워드 표현 불가
- aliases에 Config 키워드(ADTCAPS)와 사용자 검색어(SK쉴더스)가 혼재
- "Bizen"으로 검색 시 매칭되지만, 실제로는 originalToken만 Config 검색 → 다른 키워드 누락

**제안 구조 (v5.0)**:
```typescript
interface DictionaryEntry {
  id: string;
  name: string;                  // 그룹 대표 이름 (UI 표시용)
  configKeywords: string[];      // Config description에서 검색할 키워드들
  searchAliases: string[];       // 추가 사용자 검색어들
  autoGenerated: boolean;
  userEdited: boolean;
}
```

**검색 동작**:
1. **사용자 입력 매칭**: `configKeywords` ∪ `searchAliases` 전체에서 검색
2. **Config 검색**: 매칭된 entry의 `configKeywords` 전체를 OR 조건으로 검색

### 1.4 Example Transformation

**v4.3 구조**:
```json
{
  "id": "sk-shielders-001",
  "originalToken": "Bizen",
  "aliases": ["SK쉴더스", "ADTCAPS", "SKShielders", "Infosec", "ISAC"],
  "autoGenerated": true,
  "userEdited": false
}
```

**문제 시나리오**:
- 사용자 입력: "SK쉴더스" → aliases에서 매칭 → Config에서 "Bizen"만 검색 → 2개 회선만 표시
- 실제 의도: "SK쉴더스" → Config에서 Bizen, ADTCAPS, SKShielders, Infosec 모두 검색 → 모든 회선 표시

**v5.0 구조**:
```json
{
  "id": "sk-shielders-001",
  "name": "SK쉴더스",
  "configKeywords": ["Bizen", "ADTCAPS", "SKShielders", "Infosec"],
  "searchAliases": ["SK쉴더스", "ISAC", "인포섹", "에스케이쉴더스", "SK Shielders"],
  "autoGenerated": true,
  "userEdited": false
}
```

**검색 시나리오**:

| 사용자 입력 | 매칭 필드 | Config 검색 대상 |
|------------|----------|----------------|
| "Bizen" | configKeywords | Bizen, ADTCAPS, SKShielders, Infosec |
| "ADTCAPS" | configKeywords | Bizen, ADTCAPS, SKShielders, Infosec |
| "SK쉴더스" | searchAliases | Bizen, ADTCAPS, SKShielders, Infosec |
| "ISAC" | searchAliases | Bizen, ADTCAPS, SKShielders, Infosec |

---

## 2. Business Value

### 2.1 Problem Statement

**v4.3의 근본적 문제**:

1. **검색 정확도 부족**: "SK쉴더스" 검색 시 관련 회선의 일부만 표시
2. **역할 혼재**: aliases에 Config 키워드와 사용자 검색어가 섞여 있어 의도 불명확
3. **단일 originalToken 제약**: 여러 Config 키워드를 그룹화할 수 없음
4. **AI 프롬프트 복잡도**: "originalToken은 Config 검색용, aliases는 매칭용이지만 일부는 Config 검색용" 같은 복잡한 설명 필요

**실제 문제 사례** (사용자 피드백):
> "SK쉴더스를 요청해도 원본토큰에서 Bizen만 검색해서 2개의 회선만 보여주고 있어. 그런데 내가 원한 것은 Bizen, ADTCAPS, SKShielders, Infosec 등 모든 것을 config에서 확인하는 것이야."

### 2.2 Business Benefits

1. **검색 정확도 100%**: 연관된 모든 Config 키워드를 빠짐없이 검색
2. **사용자 혼란 제거**: 역할이 명확히 분리된 필드명 (configKeywords, searchAliases)
3. **유연한 그룹화**: 여러 Config 키워드를 하나의 entry로 관리
4. **AI 효율성**: 명확한 필드 역할로 프롬프트 단순화
5. **확장성**: 향후 다국어 별칭 추가 시 searchAliases만 확장

### 2.3 Target Users

- **Primary**: AI 챗봇 검색을 사용하는 사용자 (검색 정확도 향상)
- **Secondary**: 이름 사전을 입력/수정하는 사용자 (역할 명확화)
- **Tertiary**: 개발자 (코드 가독성 향상)

---

## 3. Goals and Objectives

### 3.1 Primary Goals

1. **데이터 모델 재설계**: 3개 필드 구조 (name, configKeywords, searchAliases)
2. **양방향 검색 구현**: configKeywords도 사용자 입력으로 검색 가능
3. **AI 프롬프트 재작성**: v5.0 구조에 맞게 검색 로직 명확화
4. **마이그레이션 전략**: v4.3 → v5.0 자동 변환 (사용자 판단 필요 부분 표시)
5. **이름사전 재생성**: 기존 사전 삭제 후 AI로 새로 생성

### 3.2 Secondary Goals

1. **UI 재설계**: DictionaryEditor를 3개 필드 입력으로 변경
2. **검색 성능 최적화**: 필드 역할 명확화로 불필요한 검색 제거
3. **문서 업데이트**: CLAUDE.md, systemPrompt.ts 주석 등
4. **테스트 강화**: 양방향 검색 시나리오 테스트

### 3.3 Success Metrics

| Metric | Current (v4.3) | Target (v5.0) | Measurement |
|--------|:--------------:|:-------------:|:-----------:|
| 검색 정확도 | ~50% (일부 누락) | 100% | 테스트 케이스 통과율 |
| Config 키워드 그룹화 | 불가능 (1개만) | 가능 (배열) | configKeywords 배열 크기 |
| AI 프롬프트 명확도 | 복잡 | 명확 | 프롬프트 라인 수 감소 |
| 사용자 입력 매칭 범위 | aliases만 | configKeywords + searchAliases | 테스트 시나리오 |

---

## 4. Scope

### 4.1 In Scope

#### 4.1.1 Backend Changes

- [x] **타입 정의 수정**:
  ```typescript
  // server/src/types.ts
  interface DictionaryEntry {
    id: string;
    name: string;               // 그룹 대표 이름
    configKeywords: string[];   // Config 검색 대상
    searchAliases: string[];    // 사용자 검색어
    autoGenerated: boolean;
    userEdited: boolean;
  }

  // MatchedEntry 업데이트
  interface MatchedEntry {
    matchedAlias: string;       // 실제 매칭된 키워드
    configKeywords: string[];   // Config 검색에 사용될 키워드들
    groupName: string;          // entry.name
  }
  ```

- [x] **systemPrompt.ts 재작성**:
  - v5.0 구조 설명 추가
  - 양방향 검색 로직 명확화:
    > "사용자 입력 매칭: configKeywords + searchAliases 전체에서 검색"
    > "Config 검색: 매칭된 entry의 configKeywords 전체를 OR 조건으로 검색"

- [x] **claudeClient.ts 수정**:
  - validateMatchedEntries() 로직 업데이트 (configKeywords 검증)
  - Dictionary 전송 형식 변경

- [x] **dictionaryGenerator.ts 수정**:
  - AI 생성 시 3개 필드 요청
  - 프롬프트: "각 entry는 name, configKeywords, searchAliases로 구성"

#### 4.1.2 Frontend Changes

- [x] **타입 정의 수정**:
  ```typescript
  // src/types/dictionary.ts
  interface DictionaryEntry {
    id: string;
    name: string;
    configKeywords: string[];
    searchAliases: string[];
    autoGenerated: boolean;
    userEdited: boolean;
  }

  // src/services/chatApi.ts
  interface MatchedEntry {
    matchedAlias: string;
    configKeywords: string[];
    groupName: string;
  }
  ```

- [x] **DictionaryEditor.tsx 재설계**:
  - 입력 필드 3개:
    1. name: 그룹 대표 이름 (input)
    2. configKeywords: Config 검색 키워드들 (textarea, 줄바꿈 구분)
    3. searchAliases: 추가 검색어들 (textarea, 줄바꿈 구분)
  - 도움말 툴팁:
    - configKeywords: "Config description에서 검색할 키워드들 (예: Bizen, ADTCAPS)"
    - searchAliases: "사용자가 입력할 검색어들 (예: SK쉴더스, 인포섹)"
  - "전체 삭제 후 AI 재생성" 버튼 추가

- [x] **AliasBadge.tsx 수정**:
  - 툴팁: "그룹: {name} | Config: {configKeywords.join(', ')} | 검색: {searchAliases.join(', ')}"
  - 색상 유지 (단일 gradient)

#### 4.1.3 Migration Strategy

**옵션 1: 자동 마이그레이션 (권장하지 않음)**
```typescript
// v4.3 → v5.0 자동 변환 (불완전)
function migrateV43ToV50(old: V43Entry): V50Entry {
  return {
    id: old.id,
    name: old.originalToken,
    configKeywords: [old.originalToken, ...old.aliases],  // ⚠️ 불완전
    searchAliases: old.aliases,
    autoGenerated: old.autoGenerated,
    userEdited: old.userEdited
  };
}
```

**문제**: aliases에 Config 키워드와 검색어가 혼재되어 자동 분류 불가능

**옵션 2: 전체 삭제 후 AI 재생성 (권장)**
1. 기존 사전 백업
2. 사전 삭제
3. "AI 자동 생성" 버튼으로 v5.0 구조로 새로 생성
4. 사용자가 생성 결과 검토 후 수동 수정

### 4.2 Out of Scope

- ❌ **v4.3 데이터 자동 분류**: Config 키워드와 검색어를 자동으로 구분하는 AI 분류기 (v5.1 이후 고려)
- ❌ **카테고리 재도입**: v4.3에서 제거된 category 필드는 재추가 안 함
- ❌ **다국어 지원**: 영어/일본어 별칭은 v5.1 이후 고려

### 4.3 Assumptions

- 사용자는 기존 사전을 삭제하고 재생성하는 것에 동의
- AI가 생성한 결과를 사용자가 수동으로 검토/수정할 수 있음
- configKeywords 자체도 사용자 입력으로 검색 가능해야 함

---

## 5. Requirements

### 5.1 Functional Requirements

| ID | Requirement | Priority | Status |
|----|-------------|:--------:|:------:|
| FR-01 | DictionaryEntry 타입을 name + configKeywords + searchAliases 3개 필드로 변경 | P0 | ⏳ |
| FR-02 | 사용자 입력 매칭: configKeywords + searchAliases 전체 검색 | P0 | ⏳ |
| FR-03 | Config 검색: 매칭된 entry의 configKeywords 전체 OR 조건 검색 | P0 | ⏳ |
| FR-04 | systemPrompt.ts v5.0 구조로 재작성 | P0 | ⏳ |
| FR-05 | DictionaryEditor UI 3개 필드 입력으로 재설계 | P0 | ⏳ |
| FR-06 | "전체 삭제 후 AI 재생성" 기능 추가 | P0 | ⏳ |
| FR-07 | AI 자동 생성 시 v5.0 구조로 응답 (3개 필드) | P0 | ⏳ |
| FR-08 | AliasBadge 툴팁 v5.0 구조로 표시 | P1 | ⏳ |
| FR-09 | v4.3 백업 기능 (삭제 전 자동 백업) | P1 | ⏳ |

### 5.2 Non-Functional Requirements

| ID | Requirement | Target | Priority |
|----|-------------|:------:|:--------:|
| NFR-01 | AI 재생성 시간 | < 5초 (100개 description 기준) | P0 |
| NFR-02 | 검색 정확도 | 100% (모든 관련 Config 키워드 검색) | P0 |
| NFR-03 | UI 반응성 | < 100ms (입력 필드 반응) | P1 |
| NFR-04 | 데이터 손실 방지 | 100% (백업 자동 생성) | P0 |

---

## 6. Technical Architecture

### 6.1 System Overview

```
┌─────────────────────────────────────────────────────────┐
│                  v4.3 Dictionary (Old)                   │
│  { originalToken, aliases }                              │
└─────────────────┬───────────────────────────────────────┘
                  │ 백업 생성
                  ↓
┌─────────────────────────────────────────────────────────┐
│              {fingerprint}.backup.json                   │
│  (v4.3 구조 백업)                                        │
└─────────────────────────────────────────────────────────┘
                  │
                  │ 사전 삭제
                  ↓
┌─────────────────────────────────────────────────────────┐
│            AI 자동 생성 (v5.0 구조)                      │
│  POST /api/dictionary/generate                          │
│  → Claude Sonnet 4 (systemPrompt v5.0)                 │
│  → { name, configKeywords, searchAliases }              │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│                  v5.0 Dictionary (New)                   │
│  { name, configKeywords, searchAliases }                │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│              DictionaryEditor (v5.0)                     │
│  - name 입력                                             │
│  - configKeywords 입력 (textarea)                       │
│  - searchAliases 입력 (textarea)                        │
└─────────────────────────────────────────────────────────┘
```

### 6.2 Search Flow

```typescript
// 1. 사용자 입력: "SK쉴더스"
const userInput = "SK쉴더스";

// 2. Dictionary에서 매칭 (configKeywords + searchAliases 전체 검색)
const matchedEntry = dictionary.entries.find(entry =>
  entry.configKeywords.includes(userInput) ||
  entry.searchAliases.includes(userInput)
);
// → matchedEntry = { name: "SK쉴더스", configKeywords: ["Bizen", "ADTCAPS", "SKShielders", "Infosec"], ... }

// 3. Config에서 검색 (configKeywords 전체 OR 조건)
const searchKeywords = matchedEntry.configKeywords;
// → ["Bizen", "ADTCAPS", "SKShielders", "Infosec"]

// 4. AI가 Config description에서 검색
// description includes "Bizen" OR "ADTCAPS" OR "SKShielders" OR "Infosec"
```

### 6.3 Component Hierarchy

```
DictionaryEditor (v5.0)
├── 도구 버튼
│   ├── 전체 삭제 후 AI 재생성 🆕
│   ├── 항목 추가
│   └── 백업 복원
│
├── 항목 목록 (테이블)
│   ├── name 컬럼
│   ├── configKeywords 컬럼 (배열을 ", "로 join)
│   ├── searchAliases 컬럼 (배열을 ", "로 join)
│   └── 작업 버튼 (수정, 삭제)
│
└── 추가/수정 모달
    ├── name 입력 (input)
    │   └── 도움말: "그룹 대표 이름 (예: SK쉴더스)"
    ├── configKeywords 입력 (textarea)
    │   └── 도움말: "Config description에서 검색할 키워드들\n줄바꿈으로 구분 (예: Bizen, ADTCAPS)"
    ├── searchAliases 입력 (textarea)
    │   └── 도움말: "사용자가 입력할 검색어들\n줄바꿈으로 구분 (예: SK쉴더스, 인포섹)"
    └── 저장/취소 버튼
```

---

## 7. Implementation Plan

### 7.1 Phase 1: Backend Changes (Step 1-4)

**Step 1: 타입 정의 수정**
- `server/src/types.ts`: DictionaryEntry v5.0 구조
- `src/types/dictionary.ts`: 프론트엔드 타입 동기화
- `src/services/chatApi.ts`: MatchedEntry v5.0 구조

**Step 2: systemPrompt.ts 재작성**
- v5.0 구조 설명 추가
- 양방향 검색 로직 명확화:
  ```typescript
  ## Name Dictionary 활용 (이름 사전) - v5.0

  1. **사용자 입력 매칭**: configKeywords + searchAliases 전체에서 매칭
     - 예: 사용자 입력 "Bizen" → configKeywords에서 매칭
     - 예: 사용자 입력 "SK쉴더스" → searchAliases에서 매칭

  2. **Config 검색**: 매칭된 entry의 configKeywords 전체를 OR 조건으로 검색
     - 예: ["Bizen", "ADTCAPS", "SKShielders", "Infosec"] 중 하나라도 포함

  3. **matchedEntries 생성**:
     {
       "matchedAlias": "SK쉴더스",  // 실제 매칭된 키워드
       "configKeywords": ["Bizen", "ADTCAPS", "SKShielders", "Infosec"],
       "groupName": "SK쉴더스"
     }
  ```

**Step 3: claudeClient.ts 수정**
- validateMatchedEntries() v5.0 구조 검증
- Dictionary 전송 형식 변경:
  ```typescript
  const lines = dictionary.entries.map(e => {
    const configKw = e.configKeywords.join(', ');
    const searchAl = e.searchAliases.length > 0 ? `검색어: ${e.searchAliases.join(', ')}` : '';
    return `- "${e.name}" → Config: ${configKw} ${searchAl}`;
  });
  ```

**Step 4: dictionaryGenerator.ts 수정**
- AI 생성 프롬프트 v5.0 구조로 변경
- 응답 파싱: name, configKeywords, searchAliases 추출

### 7.2 Phase 2: Frontend Changes (Step 5-7)

**Step 5: DictionaryEditor 재설계**
- `src/components/v3/DictionaryEditor.tsx`:
  - 입력 필드 3개 (name, configKeywords, searchAliases)
  - 도움말 툴팁 추가
  - "전체 삭제 후 AI 재생성" 버튼 추가

**Step 6: AliasBadge 수정**
- `src/components/v3/AliasBadge.tsx`:
  - 툴팁 내용 v5.0 구조로 변경
  - matchedAlias 표시 (어떤 키워드로 매칭되었는지)

**Step 7: 스타일 업데이트**
- `src/components/v3/DictionaryEditor.css`:
  - textarea 스타일링 (configKeywords, searchAliases)
  - 도움말 툴팁 스타일

### 7.3 Phase 3: Testing & Deployment (Step 8-10)

**Step 8: 백업 및 삭제 기능**
- 기존 v4.3 사전 자동 백업
- 사전 삭제 확인 다이얼로그
- 백업 복원 기능

**Step 9: 통합 테스트**
- 시나리오 1: 전체 삭제 후 AI 재생성 → v5.0 구조로 생성 확인
- 시나리오 2: "Bizen" 검색 → configKeywords 매칭 → 모든 관련 회선 표시
- 시나리오 3: "SK쉴더스" 검색 → searchAliases 매칭 → 모든 관련 회선 표시
- 시나리오 4: DictionaryEditor에서 항목 추가/수정 → v5.0 구조로 저장

**Step 10: 문서 업데이트**
- `CLAUDE.md`: v5.0 구조 반영
- 릴리즈 노트: v5.0.0 변경 내용 작성
- `CHANGELOG.md`: 업데이트

---

## 8. Dependencies

### 8.1 External Dependencies

- **AWS Bedrock Claude Sonnet 4**: AI 자동 생성 (v5.0 구조)
- **TypeScript**: 타입 안전성 보장

### 8.2 Internal Dependencies

- **v4.3.0 Dictionary Simplification**: 기존 2필드 구조
- **v4.2.0 AI Chatbot Alias Display**: AI 검색 기능

### 8.3 Breaking Changes

⚠️ **Breaking Change**: DictionaryEntry 타입 변경으로 기존 v4.3 사전 호환 불가
- 영향 범위: 모든 dictionary 파일
- 해결 방법: 전체 삭제 후 AI 재생성

---

## 9. Risk Analysis

### 9.1 Technical Risks

| Risk | Probability | Impact | Mitigation |
|------|:-----------:|:------:|-----------|
| AI 재생성 실패 (키워드 분류 오류) | Medium | High | 사용자 수동 검토 후 수정 가능 |
| 백업 복원 실패 | Low | High | 백업 파일 검증, 테스트 강화 |
| 검색 로직 버그 (양방향 검색) | Low | High | 단위 테스트, E2E 테스트 |
| AI 응답 형식 불일치 | Medium | Medium | 응답 파싱 로직 강화, 재시도 |

### 9.2 User Experience Risks

| Risk | Probability | Impact | Mitigation |
|------|:-----------:|:------:|-----------|
| 사용자가 3개 필드 입력 복잡하다고 느낌 | Medium | Low | 도움말 툴팁, 예시 제공 |
| AI 재생성 결과 부정확 | Medium | Medium | 수동 수정 가능, 재생성 버튼 |
| 기존 사전 손실 불안 | Low | High | 백업 자동 생성, 복원 기능 |

---

## 10. Testing Strategy

### 10.1 Unit Tests

- **DictionaryEditor.tsx**: 입력 유효성 검증 (3개 필드)
- **claudeClient.ts**: validateMatchedEntries() v5.0 구조 검증
- **systemPrompt.ts**: 양방향 검색 로직 (모의 테스트)

### 10.2 Integration Tests

- **E2E 시나리오**:
  1. 전체 삭제 후 AI 재생성 → v5.0 구조 확인
  2. "Bizen" 검색 → configKeywords 매칭 → 모든 회선 표시
  3. "SK쉴더스" 검색 → searchAliases 매칭 → 모든 회선 표시
  4. DictionaryEditor 항목 추가 → 저장 → 검색 정상 동작

### 10.3 Search Accuracy Tests

- **테스트 케이스**:
  - configKeywords 직접 검색: "Bizen" → 매칭 → 4개 키워드 모두 Config 검색
  - searchAliases 검색: "SK쉴더스" → 매칭 → 4개 키워드 모두 Config 검색
  - 없는 키워드 검색: "존재하지않는키워드" → 매칭 실패 → 빈 결과

---

## 11. Rollout Plan

### 11.1 Phase 1: Internal Testing (1 day)

- 개발 환경에서 v5.0 구조 테스트
- AI 재생성 결과 검증 (키워드 분류 정확도)
- 양방향 검색 시나리오 테스트

### 11.2 Phase 2: Beta Release (2 days)

- 일부 사용자에게 v5.0 배포
- 백업 및 복원 기능 안내
- 피드백 수집 및 버그 수정

### 11.3 Phase 3: General Availability (v5.0.0)

- 모든 사용자에게 배포
- 릴리즈 노트 공개
- 마이그레이션 가이드 제공

---

## 12. Success Criteria

### 12.1 Launch Criteria

- [x] FR-01 ~ FR-07 (P0) 모두 구현 완료
- [x] 양방향 검색 테스트 통과 (100% 정확도)
- [x] AI 재생성 테스트 통과 (v5.0 구조)
- [x] 백업 및 복원 기능 테스트 통과

### 12.2 Post-Launch Metrics (1주 후)

- 검색 정확도: 100% (모든 관련 Config 키워드 검색)
- AI 재생성 성공률: ≥ 95%
- 사용자 피드백: "검색이 정확해졌다" 긍정 피드백
- 백업 복원 성공률: 100%

---

## 13. Future Enhancements (v5.1+)

### 13.1 자동 키워드 분류

- v4.3 → v5.0 자동 마이그레이션 AI 개발
- Config 키워드와 사용자 검색어를 자동 분류

### 13.2 다국어 지원

- 영어, 일본어 별칭 추가
- searchAliases를 언어별로 분리

### 13.3 키워드 추천

- AI가 누락된 configKeywords 자동 추천
- 유사 별칭 자동 생성

---

## 14. Related Documents

- **v4.3.0 Dictionary Simplification Design**: `docs/02-design/features/dictionary-simplification.design.md`
- **v4.2.0 AI Chatbot Alias Display**: `docs/archive/2026-02/ai-chatbot-alias-display/`
- **CLAUDE.md**: 프로젝트 컨텍스트 문서

---

## 15. Migration Guide

### 15.1 For Users

**전체 삭제 후 AI 재생성** (권장):

1. DictionaryEditor 열기
2. "전체 삭제 후 AI 재생성" 버튼 클릭
3. 백업 자동 생성 확인
4. AI가 v5.0 구조로 새로 생성
5. 생성 결과 검토 후 필요 시 수동 수정

**백업에서 복원** (문제 발생 시):
1. "백업 복원" 버튼 클릭
2. 최근 백업 파일 선택
3. v4.3 구조로 복원 완료

### 15.2 For Developers

**타입 변경 사항**:
```typescript
// Old (v4.3)
interface DictionaryEntry {
  originalToken: string;
  aliases: string[];
}

// New (v5.0)
interface DictionaryEntry {
  name: string;
  configKeywords: string[];
  searchAliases: string[];
}
```

**검색 로직 변경**:
```typescript
// Old (v4.3): aliases만 매칭
const match = entry.aliases.includes(userInput);

// New (v5.0): configKeywords + searchAliases 모두 매칭
const match = entry.configKeywords.includes(userInput) ||
              entry.searchAliases.includes(userInput);
```

**Config 검색 변경**:
```typescript
// Old (v4.3): originalToken만 검색
searchInConfig([entry.originalToken]);

// New (v5.0): configKeywords 전체 검색
searchInConfig(entry.configKeywords);
```

---

## 16. Approval

| Role | Name | Date | Status |
|------|------|------|:------:|
| Product Owner | User | 2026-02-16 | ✅ Approved |
| Tech Lead | Claude Code | 2026-02-16 | ✅ Approved |

---

**Last Updated**: 2026-02-16
**Document Version**: 1.0
**Status**: 📋 Planning
